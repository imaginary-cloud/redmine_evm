<h2><%=l(:label_baseline_set)%></h2>

<%= labelled_form_for @baseline, :url => project_baselines_path(@project)  do |f| %>
  <%= render :partial => 'baselines/form', :locals => { :project => @project , :f => f } %>
  <%= submit_tag l(:button_create) %>
<% end %>