<h2><%=l(:label_baseline_plural)%></h2>

<%= labelled_form_for @baseline do |f| %>
  <%= render :partial => 'form', :locals => { :project => @project , :f => f } %>
  <%= submit_tag l(:button_save) %>
<% end %>

